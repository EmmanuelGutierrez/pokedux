import { SxProps } from "@mui/material";

interface ISearcherStyle {
  Searcher: SxProps;
}

export const SearcherStyle: ISearcherStyle = {
  Searcher: {
    margin: "auto",
    marginBottom: "30px",
  },
};
